enum TextStyleType {
  /// Display sizes - these render in the serif face by default, because the
  /// serif is what ties the page back to Ahmad's logos.
  h1,
  h2,
  h3,
  h4,

  large,
  medium,
  small,
  button,
  input,
  custom,
}
